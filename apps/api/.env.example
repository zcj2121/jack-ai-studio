# OpenAI-compatible Provider 的服务端配置
# 当前仅作为变量名示例；运行前通过服务端环境变量注入真实值
# 禁止把真实 API Key 提交到 Git
AI_PROVIDER_API_KEY=replace-with-your-api-key

# 留空时使用 OpenAI 官方 API；兼容服务可填写自己的 v1 Base URL
AI_PROVIDER_BASE_URL=

# OpenRouter 使用独立的服务端配置；Base URL 留空时使用官方兼容地址
AI_PROVIDER_OPENROUTER_API_KEY=replace-with-your-openrouter-api-key
AI_PROVIDER_OPENROUTER_BASE_URL=
